import express from "express";
import { setupApp } from "../../../src/setup-app";
describe("Blog API body validation check", () => {
  const app = express();
  setupApp(app);

  const correctTestPostAttributes = 
});
