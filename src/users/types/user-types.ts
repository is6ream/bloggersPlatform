export type UserInputModel = {
  login: string;
  password: string;
  email: string;
};

export type UserType = UserInputModel;

export type UserViewModel = {
  id: string;
  login: string;
  password: string;
  email: string;
};
